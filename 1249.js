/**
 * @param {string} s
 * @return {string}
 */
var minRemoveToMakeValid = function (s) {
  let result = [];
  let counter = 0;

  let c = s.charAt(i);

  for (let i = 0; i < s.length; i++) {

 
  }
  return result;
};
